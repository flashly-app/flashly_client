{
  "name": "flashly-app",
  "version": "0.1.0",
  "description": "all client packages for flashly app",
  "scripts": {
    "start:web": "cd packages/web-client && pnpm start",
    "start:server": "cd packages/dummy-api && pnpm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Supui Lam"
  },
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}