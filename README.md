## This is the ReadMe for Flasly Client Repo

CSS naming-guide: http://getbem.com/naming/

### Start-up Instructions
```bash
$ npm i -g pnpm # skip if you already have pnpm installed
$ pnpm i -r
$ pnpm run start:web
$ pnpm run start:server
```